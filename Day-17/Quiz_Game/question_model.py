#Creating the first class from scratch
class Question:
    def __init__(self, q_text, q_answer):
        self.text = q_text
        self.answer = q_answer
#Generating the question from the database
def get_question(User):
    get_question.text
    get_question.answer
    input("Question")

